# v21完全版 修正内容詳細

## 📋 報告された問題と対応状況

### 1. 概算見積もりアンケートの問題

| 問題 | 状態 | 対応内容 |
|-----|------|---------|
| 次へと戻るのボタンのサイズやレイアウトが崩れている | ✅ 修正完了 | `styles.css`で縦配置レイアウトを実装、ボタンサイズを最適化（最小高さ52px） |
| 最終画面で見積もり依頼ボタンをタップする前に自動的にLINE友達登録に誘導される | ✅ 修正完了 | `app.js`で送信完了後のみLINE友達登録ボタンを表示するように変更 |

### 2. 詳細見積もりフォームの問題

| 問題 | 状態 | 対応内容 |
|-----|------|---------|
| 郵便番号、住所を入力する枠がない | ✅ 修正完了 | `liff.js`に住所入力欄を追加、`routes/details.js`で住所フィールドを処理 |
| 住所欄の近くに説明文がない | ✅ 修正完了 | `liff.js`に説明文を追加、`styles.css`で黄色の背景で強調表示 |
| 立面図アップロード画面の参考画像が消えている | ✅ 修正完了 | `liff.js`に参考画像表示を追加、`public/img/elevation_sample.png`を配置 |
| アップロードした画像が大きなサムネイルで表示されていない | ✅ 修正完了 | `liff.js`にサムネイル表示機能を実装、`styles.css`でスタイル定義 |
| 建物写真はライブラリから選べない | ✅ 修正完了 | `liff.js`から`capture="environment"`属性を削除 |
| 前の画面に戻るボタンがない | ✅ 修正完了 | `liff.js`の各画面に戻るボタンを追加 |
| 入力内容確認画面で送信に失敗する | ✅ 修正完了 | `routes/details.js`に住所フィールドを追加、エラーハンドリングを改善 |

---

## 📝 ファイル別修正内容

### public/index.html

**修正内容:**
- ナビゲーションバーのクラスを`nav-vertical`に変更
- 完了メッセージのボタンに`primary`クラスを追加

**変更箇所:**
```html
<!-- 修正前 -->
<nav class="nav" id="nav-bar">

<!-- 修正後 -->
<nav class="nav-vertical" id="nav-bar">
```

---

### public/app.js

**修正内容:**
1. 最終確認画面のレンダリング変更
   - 「はい」「いいえ」ボタンを削除
   - 「次へ」ボタンを「この内容で概算見積もりを依頼」に変更
   - ボタンに`data-action="confirm-yes"`属性を追加

2. `onNext()`関数の変更
   - `data-action`属性をチェック
   - `confirm-yes`の場合は`handleConfirmYes()`を呼び出し

3. ローディング画面の実装
   - `showLoading()`関数を追加
   - `hideLoading()`関数を追加
   - 3点波形アニメーションのHTML生成

4. 送信処理の改善
   - `handleConfirmYes()`でローディング画面を表示
   - エラーハンドリングを改善
   - 送信完了後に`showDone()`を呼び出し

**主要な変更箇所:**
```javascript
// 最終確認画面のボタン
$next.textContent = 'この内容で概算見積もりを依頼';
$next.setAttribute('data-action', 'confirm-yes');

// onNext関数でのチェック
if ($next.hasAttribute('data-action') && $next.getAttribute('data-action') === 'confirm-yes') {
  handleConfirmYes();
  return;
}
```

---

### public/liff.js

**修正内容:**

1. モデルに`address`と`thumbnails`を追加
```javascript
model = {
  // ...
  form: { name: '', phone: '', postal: '', address: '' },
  thumbnails: {}
};
```

2. `renderContact()`に住所入力欄を追加
```javascript
<label>住所<span class="required">*</span></label>
<input id="address" placeholder="大阪府大阪市北区梅田1-1-1" autocomplete="address-level1" class="input-field"/>
<p class="note address-note">こちらの住所は建物の区画を確認するためにのみ利用します。ご連絡なしでの現地調査訪問は一切行っておりませんのでご安心ください。</p>
```

3. `fileInput()`関数を改善
   - サムネイルプレビュー用のdivを追加
   - `required`パラメータを追加

4. `attachFileListeners()`関数を追加
   - ファイル選択時にサムネイルを表示
   - `FileReader` APIを使用して画像をプレビュー
   - `model.thumbnails`にサムネイルHTMLを保存

5. `renderDrawings()`に参考画像を追加
```javascript
<div class="reference-image">
  <p class="note"><strong>立面図の例：</strong></p>
  <img src="/public/img/elevation_sample.png" alt="立面図の参考例" class="sample-img"/>
</div>
```

6. 全画面に戻るボタンを追加
```javascript
<a href="#" id="back" class="back-link">← 戻る</a>
```

7. 建物写真から`capture`属性を削除
```javascript
// 修正前
${fileInput('photo_front','建物の正面','image/*', true)}

// 修正後（captureパラメータを削除）
${fileInput('photo_front','建物の正面','image/*', true)}
```

8. ローディング画面の実装
   - `showLoading()`関数を追加
   - `hideLoading()`関数を追加
   - `submitAll()`でローディング画面を表示

9. 送信データに住所を追加
```javascript
fd.append('address', model.form.address);
```

---

### public/styles.css

**修正内容:**

1. 縦配置ナビゲーションのスタイル追加
```css
.nav-vertical{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:12px;
  margin-top:24px;
}
```

2. ボタンスタイルの改善
```css
.btn, button.btn{
  /* ... */
  min-height:52px;
  width:100%;
  max-width:400px;
}
```

3. ファイル入力のスタイル追加
```css
.file-input-group{
  margin:16px 0;
}

.file-input,
input[type="file"]{
  /* ... */
  border:2px dashed var(--line);
}
```

4. サムネイルプレビューのスタイル追加
```css
.thumbnail-preview{
  margin-top:12px;
}

.thumbnail-img{
  width:100%;
  max-width:300px;
  border-radius:10px;
  border:2px solid var(--line);
  box-shadow:0 2px 8px rgba(0,0,0,.1);
}
```

5. 参考画像表示のスタイル追加
```css
.reference-image{
  margin:16px 0;
  padding:16px;
  background:#fff;
  border:1px solid var(--line);
  border-radius:12px;
}

.sample-img{
  width:100%;
  max-width:400px;
  border-radius:8px;
  border:1px solid var(--line);
  margin-top:8px;
}
```

6. 住所説明文のスタイル追加
```css
.address-note{
  background:#fffbeb;
  border:1px solid #fbbf24;
  border-radius:8px;
  padding:12px;
  margin-top:8px;
  font-size:13px;
  color:#92400e;
}
```

7. ローディング画面のスタイル追加
```css
.loading-overlay{
  position:fixed;
  top:0;
  left:0;
  width:100%;
  height:100%;
  background:rgba(0,0,0,.7);
  display:flex;
  align-items:center;
  justify-content:center;
  z-index:9999;
}

/* 3点波形アニメーション */
@keyframes wave{
  0%, 80%, 100%{
    transform:scale(0);
    opacity:0.5;
  }
  40%{
    transform:scale(1);
    opacity:1;
  }
}
```

---

### routes/details.js

**修正内容:**

1. リクエストボディから`address`を取得
```javascript
// 修正前
const { leadId, name, phone, postal, lineUserId } = req.body || {};

// 修正後
const { leadId, name, phone, postal, address, lineUserId } = req.body || {};
```

2. スプレッドシートに住所を追加
```javascript
// 修正前
name || '', phone || '', postal || '', // I,J,K
'ファイルはメール添付で受領' // L

// 修正後
name || '', phone || '', postal || '', address || '', // I,J,K,L
'ファイルはメール添付で受領' // M
```

3. メール本文に住所を追加
```javascript
<p><b>詳細</b><br/>
  お名前: ${name || ''}<br/>
  電話: ${phone || ''}<br/>
  郵便番号: ${postal || ''}<br/>
  住所: ${address || ''}
</p>
```

---

### public/img/elevation_sample.png（新規）

**内容:**
- 立面図の参考画像
- 検索結果から選定した分かりやすい例

---

## 🎯 実装した機能の詳細

### 1. ローディング画面（3点波形アニメーション）

**実装方法:**
- CSS `@keyframes`を使用
- 3つのドットが順番に拡大・縮小
- `animation-delay`で時間差を設定

**表示タイミング:**
- 概算見積もり送信時
- 詳細見積もり送信時

### 2. サムネイル表示

**実装方法:**
- `FileReader` APIを使用
- `readAsDataURL()`で画像をBase64エンコード
- `model.thumbnails`オブジェクトに保存
- ステップ間の移動でも表示を維持

**対応ファイル:**
- 画像ファイル: サムネイル画像を表示
- PDFファイル: ファイル名を表示

### 3. 戻るボタン

**実装方法:**
- `model.step`をデクリメント
- `render()`を呼び出して前の画面を再表示
- 入力データとファイルは`model`に保持

**表示画面:**
- ステップ2（図面アップロード）
- ステップ3（建物写真アップロード）
- ステップ4（入力内容確認）

### 4. 住所入力欄

**実装方法:**
- `<input>`要素を追加
- `autocomplete="address-level1"`属性を設定
- バリデーションで必須チェック
- バックエンドで住所を処理

**説明文:**
- 黄色の背景で強調表示
- ユーザーの不安を解消する文言

---

## 🔍 テスト項目

### 概算見積もりアンケート

1. [ ] 各質問に回答できる
2. [ ] ボタンが縦配置で表示される
3. [ ] 最終確認画面で「この内容で概算見積もりを依頼」ボタンが表示される
4. [ ] 「はい」「いいえ」ボタンが表示されない
5. [ ] ボタンをクリックするとローディング画面が表示される
6. [ ] 送信完了後にLINE友達登録ボタンが表示される
7. [ ] LINEメッセージが届く

### 詳細見積もりフォーム

1. [ ] お名前、電話番号、郵便番号、住所の入力欄がある
2. [ ] 住所欄の下に説明文が表示される（黄色の背景）
3. [ ] 立面図アップロード画面に参考画像が表示される
4. [ ] 画像を選択するとサムネイルが表示される（最大幅300px）
5. [ ] PDFを選択するとファイル名が表示される
6. [ ] 建物写真をライブラリから選択できる
7. [ ] 各画面に戻るボタンがある
8. [ ] 戻るボタンで前の画面に戻れる
9. [ ] 戻った後も入力データとサムネイルが保持される
10. [ ] 送信ボタンをクリックするとローディング画面が表示される
11. [ ] LINEメッセージが届く
12. [ ] 管理者メールが届く（SMTP設定後）
13. [ ] メールに住所が含まれている

---

**作成日**: 2025年11月8日  
**バージョン**: v21 完全版

