# v21最終修正版v2 修正内容

## 修正した問題

### 1. NaN円の表示エラー

**原因:**
- `app.js`で`/estimate`にリクエストを送信していたが、実際のエンドポイントは`/api/estimate`

**修正内容:**
- `public/app.js`の226行目を修正
- `/estimate` → `/api/estimate`

### 2. LINE友達追加ボタンの位置

**要望:**
- 金額表示画面の下にLINE友達追加ボタンを表示
- 自動的に別の画面に遷移しない

**修正内容:**
- `showResult()`関数を修正
- 金額表示画面にLINE友達追加ボタンを追加
- `setTimeout(() => showDone(), 3000)`を削除

### 3. Cannot GET /liff/index.html エラー

**原因:**
- `liff.html`ファイルが存在しなかった
- LINEメッセージのリンクが`/liff/index.html`を指していた

**修正内容:**
- `public/liff.html`を追加
- スタイルシートとJSのパスを`/public/`に修正

---

## 修正ファイル一覧

| ファイル | 修正内容 |
|---------|---------|
| `public/app.js` | `/api/estimate`に修正、LINE友達追加ボタンを金額表示画面に追加 |
| `public/liff.html` | 新規追加（元のプロジェクトからコピー） |

---

## デプロイ手順

### 1. ファイルを上書き

1. `line-paint-estimator-v21-最終修正版v2.zip`を解凍
2. 解凍したフォルダの中身を既存のプロジェクトフォルダに**上書き**

### 2. GitHub Desktopでpush

1. **GitHub Desktop**を開く

2. 変更されたファイルを確認:
   - ✅ `public/app.js`
   - ✅ `public/liff.html`（新規）

3. コミットメッセージを入力:
   ```
   v21最終修正版v2: NaN修正、LINE友達追加ボタン位置変更、liff.html追加
   ```

4. 「**Commit to main**」をクリック

5. 「**Push origin**」をクリック

6. Renderが自動デプロイを開始（約2-3分待つ）

### 3. Renderで手動再デプロイ（重要！）

1. [Render Dashboard](https://dashboard.render.com/)にログイン

2. 「**line-paint-estimator**」サービスを選択

3. 右上の「**Manual Deploy**」ボタンをクリック

4. 「**Deploy latest commit**」を選択

5. デプロイ完了を待つ（約2-3分）

---

## 動作確認

### 概算見積もりアンケート

1. アンケートを最後まで回答
2. ローディング画面が表示される
3. **金額が正しく表示される**（NaNではない）
4. 回答内容が表示される
5. **画面の下にLINE友達追加ボタンが表示される**
6. 自動的に別の画面に遷移しない

### LINEメッセージからの遷移

1. 概算見積もり完了後、LINEメッセージが届く
2. メッセージ内のボタンをタップ
3. **詳細見積もりフォームが正しく表示される**（Cannot GETエラーが出ない）

---

## 技術的な詳細

### app.jsの修正

**修正前:**
```javascript
const res = await fetch('/estimate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(payload)
});
```

**修正後:**
```javascript
const res = await fetch('/api/estimate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(payload)
});
```

### showResult()の修正

**修正前:**
```javascript
// 3秒後にLINE友達登録画面を表示
setTimeout(() => showDone(), 3000);
```

**修正後:**
```javascript
// LINE友達追加ボタンを追加
if (addFriendUrl) {
  html += '<div style="margin-top:24px;">';
  html += `<a href="${addFriendUrl}" class="btn primary" style="display:inline-block;text-decoration:none;">LINEを開く（友だち追加）</a>`;
  html += '</div>';
}
```

### liff.htmlの追加

**内容:**
```html
<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>詳細見積のご依頼</title>
  <link rel="stylesheet" href="/public/styles.css"/>
  <script src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
</head>
<body>
  <div class="container">
    <div class="card" id="liffapp">
      <h1>詳細見積のご依頼</h1>
      <div id="liff-step"></div>
    </div>
  </div>
  <script>
    window.LIFF_CONFIG = {
      LIFF_ID: "2007914959-XP5Rpoay" 
    };
  </script>
  <script src="/public/liff.js"></script>
</body>
</html>
```

---

**作成日**: 2025年11月8日  
**バージョン**: v21 最終修正版v2

